# 验收状态

记录时间：2026-09-06。

## 已有证据

- `BuildReport.txt`：第一轮实际 Unity 场景生成成功；XR Origin 1、文档 4、物品 ID 唯一、Missing Scripts 0；四个检查点未检出重叠碰撞体；导航网格已烘焙；真实敌鱼 Swim 与两种普通鱼动画已检出。
- `Tools/Test-DeepSeaDemoCompile.ps1 -Editor`：最新运行时与 Editor 源码离线编译通过；仅 Inspector 字段默认赋值警告。

## 尚未通过

- 最新修订重新导入并生成：待用户保存当前 Unity 场景后继续。旧 BuildReport 不能覆盖后续修订。
- API Play Smoke：脚本已编写，尚未运行；不应标记任务和两种结局测试通过。
- Windows PC-VR 构建：尚未执行，没有可交付 exe。
- UniStorm：缺少已确认的 5.4+ 官方 Render Graph 支持包；天气仍未完成。
- Quest 3：尚无双眼画面、72Hz CPU/GPU 帧时间、VR 手柄全流程测试记录。
- 潜艇碰撞：Door 已定位为顶部舱盖；内部净空、铰链扫掠和进出舱仍待视觉／运行核验。
- 手套：已建立 15 个骨骼通道／手、独立左右模型；各物品精细握姿仍待头显校准。
- 物品回收：已实现超出关卡边界返回找回点；不可达内腔检测尚未完整实现。
- 场景视觉和节奏：目前为已有资产结合程序化布局的初版，未完成美术精修和15–20分钟首次游玩测时。
- 可复用预制体：已生成整套绑定装配体及主要玩家／物品／鱼类模块；所有服务、舱盖、天气和区域模块的完全独立复用仍需进一步拆分验证。

## 本次发现的旧逻辑副作用

首次刷新时，旧 `DeepSeaStalkerSceneInstaller` 自动重建并保存了 `1-VR` 的测试 AI。没有可靠的本轮前磁盘快照可用于只恢复该自动写入，因此没有回退整场景或覆盖用户原有脏改动。旧自动安装入口现已关闭，保留显式菜单。

## 安全暂停原因

编辑器刷新被安全审核拦截：当前 Unity 场景可能存在未保存状态。已请求用户先保存或另存备份，之后才能继续导入、切换测试场景及打包。没有通过其他工具绕过拦截。
